let n1 = 10;
let n2 = 20;

// ++ é o operador de incremento (aumenta 1 no valor da variável antes de mostrar)
console.log(++n1);

// -- é o operador de decremento (diminui 1 no valor da variável antes de mostrar)
console.log(--n2);

